---
title: Styling UploadShad
description: Learn how to style UploadShad components using the styling API
---

## Overview

UploadShad allows custom styling for the container of **FileUploader** and **UploadedFiles** components, using the **className** props.

Further custom styling of FileUploader, and UploadedFiles are coming soon.

### Implementation

<br />

```tsx
const UploadShad = React.forwardRef<HTMLInputElement, UploadShadProps>(
  ({ className, type, defaultValues, handleChange, maxFiles, maxSize, customLoader, ...props }, ref) => {
    return (
      <UploadContextProvider defaultImages={defaultValues} handleChange={handleChange}>
        {/* Passing down Tailwind styles */}
        <div className={cn("w-full flex justify-center items-center gap-5 flex-col", className)}>
          <FileUploader maxFiles={maxFiles} maxSize={maxSize} multiple />
          <UploadedFiles customLoader={customLoader} />
        </div>
      </UploadContextProvider>
    );
  }
);
UploadShad.displayName = "ImagesInput";
```

### Usage

<br />

```tsx
<UploadShad
  // ...Other Required props
  className="w-full"
/>
```

## That's It

Great! Now you can use Tailwind styling to customize UploadShad.
